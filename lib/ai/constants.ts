// 2단계(FindFit 자체 누적 데이터 벤치마크)로 승격하기 위한 카테고리+단계별
// 최소 표본 수. 초기 데이터의 실제 분산을 관측한 뒤 통계적으로 확정할
// 예정 — 지금은 임시값이며, 이 값 미만이면 3단계(AI 추정치 표시)로 폴백한다.
export const MIN_BENCHMARK_SAMPLE_SIZE = 50

// benchmark_comment(리포트 화면에 노출되는 "동일 카테고리 평균 대비" 문구)는
// 위 2단계 승격 기준(50건)까지 기다리면 사실상 영원히 "비교 데이터 부족"만
// 노출된다 — 이 문구 자체는 훨씬 작은 표본으로도 정직하게 "N건 기준 평균"이라고
// 밝히면 되므로 별도의 낮은 임계값을 둔다.
export const MIN_BENCHMARK_COMMENT_SAMPLES = 3
